.row-fluid
  .span6
    %h2 Create
    .new_feedback
      :javascript
        $.ajax({
          url : '#{new_feedback_path}',
          dataType : 'script'
        });
  .span6
    %h2 Browse Feedback
    .feedback-list
      :javascript
        $.ajax({
          url : '#{feedback_index_path}',
          dataType : 'script'
        });
.row-fluid
  .span6
    %h2 Review
    .new_meta_feedback
      :javascript
        $.ajax({
          url : '#{new_meta_feedback_path}',
          dataType : 'script'
        });
  .span6
    %h2
      Stack Rankings
      %a.carousel-control.left{"data-slide" => "prev", :href => "#stack-rankings"} ‹
      %a.carousel-control.right{"data-slide" => "next", :href => "#stack-rankings"} ›

    .ranking-list
      :javascript
        $.ajax({
          url : '#{rankings_path}',
          dataType : 'script'
        });

