%h2 Your Circles
#circles
  - @circles.each do |circle|
    .circle{id: circle.name.parameterize, 'data-text' => circle.name, 'data-link' => circle_path(circle)}

:javascript
  $('.circle').GPlusCircle();
