= link_to(new_loop_kickoff_path, class: 'btn btn-large') do
  %i.icon-plus
  Kickoff a new loop

%h2 Loops that you need to fill out
%ul
- @assigned_loops.each do |loop|
  %li
    = link_to(loop_path(loop)) do
      A loop about
      = loop.subject_email

%h2 Loops that you created
%ul
- @created_loops.each do |loop|
  %li
    = link_to(loop_path(loop)) do
      = "A loop about #{loop.subject_email}, assigned to #{loop.email}"