<?xml version="1.0" encoding="UTF-8" ?>
<ApplicationManifest xmlns="http://schemas.google.com/ApplicationManifest/2009">

  <!-- Support info to show in the marketplace & control panel -->
  <Support>
    <!-- URL for application setup as an optional redirect during the install -->
    <!--<Link rel="setup" href="http://www.loopb.ac/google/setup?domain=${DOMAIN_NAME}"/>-->

    <!-- URL for application configuration, accessed from the app settings page in the control panel -->
    <Link rel="manage" href="http://www.loopb.ac/google/admin?domain=${DOMAIN_NAME}"/>

    <!-- URL explaining how customers get support. -->
    <Link rel="support" href="http://www.loopb.ac/google/support"/>

    <!-- URL that is displayed to admins during the deletion process, to specify policies such as data retention, how to claim accounts, etc. -->
    <Link rel="deletion-policy" href="http://www.loopb.ac/google/deletion-policy"/>

    <!-- URL that is displayed to customers so that they may purchase additional seats. -->
    <Link rel="add-seats" href="http://www.loopb.ac/google/purchase"/>
  </Support>

  <!-- Name and description pulled from message bundles -->
  <Name>Loopb.ac</Name>
  <Description>Transform the way your people grow at work.</Description>

  <!-- Show this link in Google's universal navigation for all users -->
  <Extension id="navLink" type="link">
    <Name>Loopb.ac</Name>
    <Url>https://www.loopb.ac/home?from=google&amp;domain=${DOMAIN_NAME}</Url>
    <!-- This app also uses the Calendar API -->
    <Scope ref="calendarFeed"/>
  </Extension>

  <!-- Declare our OpenID realm so our app is white listed -->
  <Extension id="realm" type="openIdRealm">
    <Url>https://loopb.ac</Url>
  </Extension>

  <!-- Need access to the Calendar feed -->
  <Scope id="calendarFeed">
    <Url>https://www.google.com/calendar/feeds/</Url>
    <Reason>This application creates calendar events for one on ones and reviews</Reason>
  </Scope>

  <!-- Need access to the Profiles feed -->
  <Scope id="provisionUserFeed">
    <Url>https://apps-apis.google.com/a/feeds/user/#readonly</Url>
    <Reason>This application provides autocomplete functionality when collaborating</Reason>
  </Scope>

  <!-- Gmail contextual gadget extractor -->

  <Extension id="extractor" type="contextExtractor">
    <Name>Hello World</Name>
    <Url>google.com:HelloWorld</Url>
    <Triggers ref="gadget"/>
    <Scope ref="emailSubject"/>
    <Scope ref="emailBody"/>
    <Container name="mail"/>
  </Extension>

  <!-- Gmail contextual gadget -->

  <Extension id="gadget" type="gadget">
    <Name>Hello World Gmail contextual gadget</Name>
    <Url>http://example.com/gadgets/hello_world_gadget.xml</Url>
    <Container name="mail"/>
  </Extension>

  <!-- Gmail contextual gadget scopes -->

  <Scope id="emailSubject">
    <Url>tag:google.com,2010:auth/contextual/extractor/SUBJECT</Url>
    <Reason>This application searches the Subject: line of each email
      for the text "Hello World."
    </Reason>
  </Scope>

  <Scope id="emailBody">
    <Url>tag:google.com,2010:auth/contextual/extractor/BODY</Url>
    <Reason>This application searches the message body of each email
      for the text "Hello World."
    </Reason>
  </Scope>

  <!-- Configures extensions available for the standard edition of the application. -->
  <Edition id="free">
    <Name>Free Edition</Name>
    <Extension ref="navLink"/>
    <Extension ref="realm"/>
  </Edition>

  <!-- Configures extensions available for the pro edition of the application -->
  <Edition id="pro">
    <Name>Pro Edition</Name>
    <Extension ref="navLink"/>
    <Extension ref="realm"/>
    <Extension ref="gadget"/>
    <Extension ref="extractor"/>
  </Edition>

</ApplicationManifest>